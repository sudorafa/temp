export enum VibbraTables {
  USERS = 'users',
  AUTHENTICATED_USERS = 'authenticated_users',
  COMPANIES = 'companies',
  EXPENSE_CATEGORIES = 'expense_categories',
  USER_SETTING = 'users_settings',
  INVOICES = 'invoices',
  EXPENSES = 'expenses',
}
